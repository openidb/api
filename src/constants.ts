/**
 * Shared constants across the application
 */

// Embedding dimensions for Gemini embedding-001 model
export const EMBEDDING_DIMENSIONS = 3072;

// Embedding dimensions for Jina embeddings-v3 model (Matryoshka, configurable 256–2048)
export const JINA_EMBEDDING_DIMENSIONS = 1024;

// Embedding dimensions for Nemotron (nvidia/llama-nemotron-embed-vl-1b-v2)
export const NEMOTRON_EMBEDDING_DIMENSIONS = 2048;
